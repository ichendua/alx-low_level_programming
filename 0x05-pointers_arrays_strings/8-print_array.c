#include "main.h"
#include <stdio.h>

/**
 * print_array - prints n element of array
 * @a: assigned int
 * @n: next assigned int
 * Return: nothing
 */

void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n ; i++)
	{
	if (i != n - 1)
	{
	printf("%d, ", a[inc]);
	}	
	else
	printf("%d", a[inc]);
	}
	printf("\");
}
