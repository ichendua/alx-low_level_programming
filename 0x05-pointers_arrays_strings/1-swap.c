#include "main.h"

/**
 * swap_int - function that swaps the values of two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: Always 0.
 */
void swap_int(int *a, int *b);
{	
	int a;
	int b;

	int a = 98
	int *a = &a
	int *b = a
}

