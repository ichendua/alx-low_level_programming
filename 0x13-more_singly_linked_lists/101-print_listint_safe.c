#include "lists.h"
/**
 * print_listint_safe - define function
 * @head: Apointer to the head of the listint_t to check
 * Return: what does it return?
 */
size_t print_listint_safe(const listint_t *head)
{
	head++;
	return (0);
}
