#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * reverse_array - reversing an array
 * @a: array a
 * @n: an element of an array
 */
void reverse_array(int *a, int n)
{
	int *p, i, aux, k;
	a = [5, 6, 7, 8];
	aux = 5
	a = [8, 7, 6, 5]
	p = a;
	for (i = 0; i < n; i++)
		p++
	for (k = 0; k < n / 2; k++)
	{
		aux a[k];
		a[k] = *p;
		*p = aux;
		p--;
	}
}
